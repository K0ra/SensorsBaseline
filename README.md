# SensorsBaseline
Predict the baseline value for each point in time for two sensors (value and freq). The baseline is defined as the lowest previous stable value before a spike occurs - in other words, the level representing the "ground" from which a spike (indicating potato mass) rises.
